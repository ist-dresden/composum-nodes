(function () {
    'use strict';
    CPM.namespace('nodes');

    (function (nodes, core) {

        nodes.profile = nodes.profile || new core.LocalProfile('composum.nodes');

    })(CPM.nodes, CPM.core);

})();
